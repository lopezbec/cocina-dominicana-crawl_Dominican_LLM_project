# Dominican Culture Scraper Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# LOCAL FIRECRAWL CONFIGURATION
# =============================================================================

# Firecrawl API URL (Local Docker setup)
# Default: http://localhost:3002
# Do not change unless you modified docker-compose.yml ports
FIRECRAWL_API_URL=http://localhost:3002

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Logging Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# Output Directory
# Directory where scraped content will be saved
# Default: scraped_content
# OUTPUT_DIR=scraped_content

# Scraping Configuration
# These settings can be overridden in config.yaml
# Base delay between requests in seconds (local has no rate limits)
# Default: 0.5 seconds
# SCRAPE_DELAY=0.5

# Maximum retry attempts for failed requests
# Default: 3 attempts
# MAX_RETRIES=3

# Maximum crawl depth for category crawling
# Default: 2 levels
# MAX_CRAWL_DEPTH=2

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 
# 1. Start Firecrawl services:
#    docker-compose up -d
#
# 2. Verify services are running:
#    curl http://localhost:3002/test
#    Expected response: Hello, world!
#
# 3. Run the scraper:
#    python scraper.py
#
# 4. Stop services when done:
#    docker-compose down
#
# See local-setup.md for detailed instructions and troubleshooting

# =============================================================================
# PERFORMANCE TUNING
# =============================================================================
#
# To adjust scraping speed, edit firecrawl.env:
#
# NUM_WORKERS_PER_QUEUE=8   (default, balanced)
# NUM_WORKERS_PER_QUEUE=16  (faster, more resources)
# NUM_WORKERS_PER_QUEUE=32  (very fast, high resources)
#
# After changes: docker-compose restart firecrawl-api

# =============================================================================
# TROUBLESHOOTING
# =============================================================================

# Services won't start:
# - Check Docker is running: docker ps
# - View logs: docker-compose logs -f
# - Reset: docker-compose down -v && docker-compose up -d

# API not responding:
# - Check health: docker-compose ps
# - Test endpoint: curl http://localhost:3002/test
# - Restart: docker-compose restart firecrawl-api

# Port conflicts:
# - Edit docker-compose.yml and change port mappings
# - Example: "3003:3002" instead of "3002:3002"

# For other issues:
# - Check scraping.log for detailed error messages
# - Review local-setup.md for common solutions
# - File an issue on GitHub if needed